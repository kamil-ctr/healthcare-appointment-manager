# ---------------------------------------------------------------
# Copy to web/.env and fill in.  NEVER commit the real .env.
# ---------------------------------------------------------------

# Base URL of the API. Leave blank for same-origin dev proxying, or point
# at the deployed backend, e.g. https://healthcare-api.example.com
VITE_API_URL=http://localhost:4000
