{
  "name": "healthcare-appointment-manager-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Backend API for the Healthcare Appointment & Follow-up Manager",
  "engines": {
    "node": ">=20.6.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch --env-file=.env src/index.js",
    "migrate": "node --env-file=.env src/db/migrate.js",
    "migrate:prod": "node src/db/migrate.js",
    "seed:admin": "node --env-file=.env scripts/seed-admin.js",
    "seed:demo": "node --env-file=.env scripts/seed-demo.js",
    "smoke-test": "node --env-file=.env scripts/smoke-test.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "nodemailer": "^9.0.5",
    "pg": "^8.13.1"
  }
}
